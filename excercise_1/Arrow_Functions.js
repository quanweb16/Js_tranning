// Ví dụ về hàm mũi tên trong JavaScript
let greet = (name) => {
    console.log("Hello, " + name + "!");
  };
  
  greet("Alice"); // Hello, Alice!
  
  let add = (a, b) => a + b;
  let result = add(10, 20); // 30